# Модуль 6. Модель с точностью 99%, которая не работает

> **Уровень:** Junior · **Время:** ~90 минут · **Тема:** метрики классификации

---

## 🏢 Ситуация

Стажёр из соседней команды хвастается в общем чате: «Обучил антифрод-модель, accuracy 99.2%!» Игорь присылает вам личное сообщение:

> «Посмотри его ноутбук и найди подвох. Подсказка: фрода в данных 0.8%. Потом расскажешь на демо — это лучший урок про метрики, который можно получить.»

Вы открываете ноутбук: модель предсказывает «не фрод» **всегда**. Accuracy = 99.2%, потому что честных транзакций 99.2%. Модель бесполезна — она не поймала ни одного мошенника.

## 🎯 Ваша задача

1. Разобраться в матрице ошибок и метриках: precision, recall, F1.
2. Понять ROC-AUC и PR-AUC.
3. Научиться выбирать метрику под цену ошибки в бизнесе.

---

## 📚 Теория

### Матрица ошибок

Для бинарной классификации (фрод = позитивный класс):

|  | Предсказано: фрод | Предсказано: не фрод |
| --- | --- | --- |
| **Реально фрод** | TP (поймали) | **FN (пропустили мошенника)** |
| **Реально не фрод** | **FP (заблокировали честного)** | TN |

Две ошибки — два разных ущерба:
- **FN** — пропущенный фрод: компания теряет деньги напрямую.
- **FP** — заблокированный честный клиент: испорченный опыт, звонки в поддержку, отток.

### Основные метрики

| Метрика | Формула | Отвечает на вопрос |
| --- | --- | --- |
| **Accuracy** | (TP+TN)/все | «Какая доля предсказаний верна?» — обманчива при дисбалансе |
| **Precision** | TP/(TP+FP) | «Из помеченных как фрод — сколько реально фрод?» |
| **Recall** | TP/(TP+FN) | «Из всего фрода — сколько поймали?» |
| **F1** | гарм. среднее P и R | компромисс, когда важны оба |

**Precision и recall всегда в противоречии:** снижаем порог → ловим больше фрода (recall ↑), но чаще блокируем честных (precision ↓).

<figure class="viz">
<svg viewBox="0 0 700 260" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Матрица ошибок и ROC-кривая">
  <text x="180" y="20" text-anchor="middle" fill="#e8ecf8" font-size="13" font-weight="bold">Матрица ошибок</text>
  <g font-size="12" text-anchor="middle">
    <rect x="90" y="60" width="120" height="75" fill="rgba(52,211,153,.18)" stroke="#34d399"/>
    <text x="150" y="92" fill="#34d399" font-weight="bold">TP</text><text x="150" y="110" fill="#93a0c4" font-size="10.5">поймали фрод</text>
    <rect x="212" y="60" width="120" height="75" fill="rgba(248,113,113,.18)" stroke="#f87171"/>
    <text x="272" y="92" fill="#f87171" font-weight="bold">FN</text><text x="272" y="110" fill="#93a0c4" font-size="10.5">пропустили! −$$$</text>
    <rect x="90" y="137" width="120" height="75" fill="rgba(248,113,113,.12)" stroke="#f87171" stroke-dasharray="4 3"/>
    <text x="150" y="169" fill="#f87171" font-weight="bold">FP</text><text x="150" y="187" fill="#93a0c4" font-size="10.5">заблокировали честного</text>
    <rect x="212" y="137" width="120" height="75" fill="rgba(147,160,196,.12)" stroke="#93a0c4"/>
    <text x="272" y="169" fill="#93a0c4" font-weight="bold">TN</text><text x="272" y="187" fill="#93a0c4" font-size="10.5">верно пропустили</text>
    <text x="150" y="50" fill="#93a0c4" font-size="10.5">предсказано: фрод</text>
    <text x="272" y="50" fill="#93a0c4" font-size="10.5">предсказано: не фрод</text>
    <text x="70" y="100" fill="#93a0c4" font-size="10.5" transform="rotate(-90 70 100)">реально фрод</text>
    <text x="70" y="177" fill="#93a0c4" font-size="10.5" transform="rotate(-90 70 177)">не фрод</text>
  </g>
  <text x="530" y="20" text-anchor="middle" fill="#e8ecf8" font-size="13" font-weight="bold">ROC-кривая</text>
  <line x1="410" y1="220" x2="670" y2="220" stroke="#93a0c4"/>
  <line x1="410" y1="220" x2="410" y2="45" stroke="#93a0c4"/>
  <line x1="410" y1="220" x2="665" y2="50" stroke="#93a0c4" stroke-dasharray="5 5"/>
  <path d="M 410 220 C 430 110 470 70 660 52" fill="none" stroke="#6c8cff" stroke-width="2.5"/>
  <text x="480" y="95" fill="#6c8cff" font-size="11.5">AUC = 0.83</text>
  <text x="600" y="180" fill="#93a0c4" font-size="10.5">монетка: AUC = 0.5</text>
  <text x="540" y="240" text-anchor="middle" fill="#93a0c4" font-size="11">FPR →</text>
  <text x="398" y="130" text-anchor="middle" fill="#93a0c4" font-size="11" transform="rotate(-90 398 130)">TPR (recall) →</text>
</svg>
<figcaption>Слева: четыре исхода классификации — у FN и FP разная цена. Справа: ROC-кривая показывает качество при всех порогах сразу; диагональ — случайное угадывание.</figcaption>
</figure>

### ROC-AUC и PR-AUC

- **ROC-кривая:** TPR (recall) против FPR при всех порогах. **AUC** — площадь под ней: вероятность, что случайный фрод получит скор выше случайной честной транзакции. 0.5 — монетка, 1.0 — идеал.
- ROC-AUC не зависит от порога и удобна для сравнения моделей, **но** при сильном дисбалансе может выглядеть оптимистично.
- **PR-AUC** (precision-recall) честнее при редком позитивном классе — используйте её для фрода, дефектов, редких болезней.

### Как выбирать метрику: от цены ошибки

Алгоритм зрелого инженера:

1. Выписать цену FP и цену FN в деньгах/последствиях.
2. Понять операционное ограничение (сколько кейсов может разобрать команда ручной проверки?).
3. Выбрать метрику: например, «recall при precision ≥ 90%» или «precision в топ-1000 алертов в день».

---

## 💻 Практика

Возьмём модель оттока из модуля 5 и посмотрим на неё правильно.

```python
from sklearn.metrics import (confusion_matrix, precision_score, recall_score,
                             roc_auc_score, average_precision_score,
                             precision_recall_curve)

pred_05 = (proba >= 0.5).astype(int)
print(confusion_matrix(y_test, pred_05))
# [[36214   306]
#  [ 2101  1379]]   ← при пороге 0.5 пропускаем 2101 из 3480 уходящих (recall 40%)

print(f"precision={precision_score(y_test, pred_05):.2f}  recall={recall_score(y_test, pred_05):.2f}")
# precision=0.82  recall=0.40

print(f"ROC-AUC = {roc_auc_score(y_test, proba):.3f}")            # 0.834
print(f"PR-AUC  = {average_precision_score(y_test, proba):.3f}")  # 0.581
```

### Подбираем порог под бизнес

Ретеншен может обработать 5000 звонков; каждый удержанный клиент приносит 12 000 ₽, звонок стоит 150 ₽.

```python
prec, rec, thr = precision_recall_curve(y_test, proba)
# ищем порог, при котором в алерты попадает ~5000 клиентов теста
# → порог 0.31: precision 0.55, recall 0.79
# Экономика: 5000 звонков * 150₽ = 750K; удержим ~0.55*5000*0.3 (конверсия звонка) * 12000₽ ≈ 9.9M
```

При пороге 0.5 мы «экономили» на звонках, но теряли 60% уходящих. Порог 0.31 хуже по precision — и **лучше по деньгам**.

<details>
<summary><b>Ответ стажёру (и вывод для демо)</b></summary>

1. Accuracy 99.2% при базовой доле честных 99.2% — это качество **константного предсказания**. Метрика не отличила модель от заглушки.
2. Правильный набор для антифрода: матрица ошибок + PR-AUC + «recall при заданном precision», порог — из экономики.
3. Всегда сравнивайте модель с **тупым бейзлайном** (константа, случайность): если не побеждает — модели нет.

Игорь: «Именно. Первое, что я спрашиваю на собеседованиях: "accuracy 99% — это хорошо?" Правильный ответ начинается со слов "зависит от баланса классов и цены ошибки".»
</details>

---

## 📌 Запомнить

- Accuracy лжёт при дисбалансе классов.
- Precision = чистота алертов, Recall = полнота поимки; они всегда в трейд-оффе.
- ROC-AUC — сравнение моделей; PR-AUC — честность при редком классе.
- Порог выбирается из цены ошибок и операционных ограничений, а не «0.5 по умолчанию».

**Что почитать:** Andrew Ng, *ML Specialization*, курс 2 (skewed datasets, precision/recall); документация sklearn «Model evaluation».

Квиз ниже!
