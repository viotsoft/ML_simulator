# Модуль 9. Инцидент: модель развалилась в проде

> **Уровень:** Junior+ · **Время:** ~90 минут · **Тема:** валидация, переобучение, утечки данных

---

## 🏢 Ситуация

Четверг, 11:00. В канале #ml-alerts тревога: скоринг-модель соседней команды, показавшая на тесте ROC-AUC 0.97, в проде выдаёт 0.61 — хуже, чем старые ручные правила. CTO Виктор требует разбора. Лена назначает вас в инцидент-группу:

> «Лучший способ научиться валидации — расследовать чужой фейл. Найди, где именно они себя обманули. Смотри на три вещи: как делили данные, что попало в признаки и когда считались статистики.»

## 🎯 Ваша задача

1. Понять переобучение и недообучение через bias/variance.
2. Освоить кросс-валидацию и валидацию по времени.
3. Составить каталог утечек данных — и найти утечку в инциденте.

---

## 📚 Теория

### Переобучение и недообучение

- **Недообучение (high bias):** модель слишком проста, плоха и на train, и на тесте.
- **Переобучение (high variance):** модель выучила шум; на train блестяще, на новых данных — провал.

Диагностика по разрыву: `train_metric` ≫ `val_metric` → переобучение. Обе низкие → недообучение. Andrew Ng: «Смотрите на train error и val error как врач на два анализа — сочетание говорит о диагнозе».

Лечение переобучения: больше данных, проще модель, регуляризация, early stopping. Лечение недообучения: сложнее модель, лучше признаки, дольше обучение.

<figure class="viz">
<svg viewBox="0 0 700 250" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Кривые обучения: переобучение">
  <text x="350" y="22" text-anchor="middle" fill="#e8ecf8" font-size="13" font-weight="bold">Диагноз по двум кривым: train vs validation</text>
  <line x1="60" y1="210" x2="660" y2="210" stroke="#93a0c4"/>
  <line x1="60" y1="210" x2="60" y2="40" stroke="#93a0c4"/>
  <path d="M 70 70 C 180 130 300 165 650 185" fill="none" stroke="#6c8cff" stroke-width="2.5"/>
  <path d="M 70 75 C 180 125 260 118 350 118 C 460 118 560 70 650 48" fill="none" stroke="#f87171" stroke-width="2.5"/>
  <line x1="350" y1="45" x2="350" y2="205" stroke="#34d399" stroke-width="2" stroke-dasharray="6 4"/>
  <text x="350" y="38" text-anchor="middle" fill="#34d399" font-size="12" font-weight="bold">early stopping — здесь</text>
  <text x="600" y="175" fill="#6c8cff" font-size="12">train error ↓↓</text>
  <text x="565" y="60" fill="#f87171" font-size="12">validation error ↑</text>
  <text x="180" y="95" fill="#93a0c4" font-size="11">учимся честно:</text>
  <text x="180" y="110" fill="#93a0c4" font-size="11">обе кривые падают</text>
  <text x="500" y="150" fill="#93a0c4" font-size="11">переобучение: запоминаем</text>
  <text x="500" y="165" fill="#93a0c4" font-size="11">шум train-выборки</text>
  <text x="360" y="232" text-anchor="middle" fill="#93a0c4" font-size="11.5">сложность модели / эпохи обучения →</text>
  <text x="48" y="125" text-anchor="middle" fill="#93a0c4" font-size="11" transform="rotate(-90 48 125)">ошибка →</text>
</svg>
<figcaption>Пока обе кривые падают — модель учится; когда validation разворачивается вверх при падающем train — началось запоминание шума. Разрыв кривых = переобучение.</figcaption>
</figure>

### Кросс-валидация

Один train/test split — одна случайная оценка. **K-fold CV:** делим данные на K частей, K раз обучаемся на K−1 частях и валидируемся на оставшейся; усредняем. Получаем оценку и её разброс.

- `StratifiedKFold` — при дисбалансе классов.
- **`TimeSeriesSplit` / split по времени** — если данные имеют время (почти всегда в бизнесе!). Обучаемся на прошлом, валидируемся на будущем. Случайный сплит транзакций за 2 года = модель «видела будущее».
- **GroupKFold** — если у одного клиента много строк: все строки клиента должны попасть в один фолд, иначе модель узнаёт клиента, а не закономерность.

### Каталог утечек данных (выучить наизусть)

1. **Утечка таргета в признаки:** признак — следствие таргета («число звонков коллекторов» для дефолта).
2. **Утечка через время:** признаки/агрегаты посчитаны с использованием будущего (не point-in-time).
3. **Утечка через препроцессинг:** scaler/encoder/imputer обучен на всех данных до сплита.
4. **Утечка через дубликаты/группы:** один клиент и в train, и в test.
5. **Утечка через отбор признаков:** feature selection сделан на всех данных, потом «честная» CV.

Симптом всех утечек одинаков: **подозрительно высокая метрика офлайн и провал в проде.**

---

## 💻 Практика: расследование

Изучаете ноутбук упавшей модели.

```python
# УЛИКА 1: случайный сплит временных данных
X_train, X_test = train_test_split(df, test_size=0.2, random_state=42)
# заявки за 2 года перемешаны: модель обучалась на декабре, тестировалась на январе того же года

# УЛИКА 2: препроцессинг до сплита
scaler = StandardScaler().fit(X)          # ← на ВСЕХ данных
X_scaled = scaler.transform(X)
X_train, X_test = train_test_split(X_scaled, ...)

# УЛИКА 3 (главная): признак из будущего
features = [..., "account_status_at_export"]  
# статус аккаунта НА МОМЕНТ ВЫГРУЗКИ данных, а не на момент заявки.
# У дефолтников статус "blocked" — модель просто читает ответ.
```

Проверка гипотезы об утечке:

```python
from sklearn.model_selection import cross_val_score
# честная валидация: split по времени + без утечного признака
auc_leaky   = 0.97   # как у них
auc_no_leak = cross_val_score(model, X.drop(columns=["account_status_at_export"]),
                              y, cv=TimeSeriesSplit(5), scoring="roc_auc").mean()
print(auc_no_leak)   # 0.74 ← настоящее качество модели
```

Пермутационный тест на утечку: важность одного признака 0.61 из 1.0 — один признак «объясняет» всё. Признак, который слишком хорош, почти всегда утечка.

<details>
<summary><b>Отчёт по инциденту (постмортем)</b></summary>

**Причина:** признак `account_status_at_export` содержит информацию из будущего (статус проставляется после дефолта). Дополнительно: случайный сплит временных данных и препроцессинг до сплита завысили оценку.

**Настоящее качество модели:** ROC-AUC 0.74 (а не 0.97). В прод модель вышла «слепой»: в реальном времени признак-подсказка всегда равен "active".

**Исправления (процессные, не только технические):**
1. Валидация по времени — стандарт для всех моделей с временной природой.
2. Весь препроцессинг — только внутри `Pipeline`.
3. Чек на ревью: «каждый признак доступен на момент предсказания?» — обязательный пункт.
4. Алерт: если один признак даёт > 40% важности — ручная проверка на утечку.

Виктор: «Отличный постмортем. Заметьте: никого не наказываем — чиним процесс.»
</details>

---

## 📌 Запомнить

- Разрыв train/val метрик — главный индикатор переобучения.
- Данные со временем валидируются по времени; строки одного клиента не разлучаем между фолдами и не раскидываем в train/test.
- Слишком хорошая метрика — повод для тревоги, а не радости.
- Препроцессинг — только в Pipeline, признаки — только point-in-time.

**Что почитать:** Andrew Ng, *ML Specialization*, курс 2 (bias/variance, ML development process); статья «Leakage in Data Mining» (Kaufman et al.).

Квиз ниже!
